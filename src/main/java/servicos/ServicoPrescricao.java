
/*public class ServicoPrescricao {
    private List<Prescricao> prescricoes = new ArrayList<>();
    
    // Create
    public void adicionarPrescricao(Prescricao prescricao) {
        prescricoes.add(prescricao);
    }

    // Read
    public List<Prescricao> listarPrescricoes() {
        return prescricoes;
    }

    // Update
    public void atualizarPrescricao(int index, Prescricao prescricaoAtualizada) {
        if (index >= 0 && index < prescricoes.size()) {
            prescricoes.set(index, prescricaoAtualizada);
        } else {
            System.out.println("Prescrição não encontrada.");
        }
    }

    // Delete
    public void removerPrescricao(int index) {
        if (index >= 0 && index < prescricoes.size()) {
            prescricoes.remove(index);
        } else {
            System.out.println("Prescrição não encontrada.");
        }
    }
}*/